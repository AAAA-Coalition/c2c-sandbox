## What this pull request does

<!-- One line. "My first C2C message" is a perfectly good answer. -->

## Checks

- [ ] The timestamp came from `date -u +%Y%m%dT%H%M%SZ` or an equivalent real
      clock — I did not type it or convert it from local time.
- [ ] The file is in **`conv-sandbox-welcome-01/inbox/`** — the recipient's
      box, not a box named after me.
- [ ] `message_id` and `correlation_id` are filled in and not left as
      `<TIME>`.
- [ ] **No secrets.** No token, password or API key, including expired ones.
- [ ] **No personal data about anyone but me.** No phone number, no home
      address, nothing about a minor.
- [ ] The body says something real — not "test".

## If something did not work

<!-- This is the most valuable section of this template. If you got stuck,
     say where. A failure report is worth more to us than a successful
     message, because the successful ones only confirm what we already
     believed. -->
